# Changelog

白鸽五号的代码、策略、执行、数据库与部署变更统一记录在这里。

版本号采用语义化版本：

- 主版本：策略职责或交易行为发生不兼容变化。
- 次版本：新增向后兼容的策略或执行能力。
- 修订版：不改变预期策略行为的修复、测试和维护。

## [5.1.0] - 2026-09-25

### Added

- 为五号核心下单、减仓、止损、移动止盈和过渡仓规则建立行为回归测试。
- 测试环境使用独立占位密钥，不再依赖生产 `service.env`。
- 新增稳定的交易执行网关，统一表达开仓、减仓和原生止损刷新请求。
- 引入 Alembic 迁移基线、默认只预演的数据保留工具和只读重复文件审计工具。
- 生成四号/五号内容哈希重复清单和分批公共包收敛方案；本次不删除生产文件。

### Changed

- 五号策略引擎通过执行网关调用原有交易服务；订单类型、方向映射、落账和止损行为保持不变。
- 将无副作用的退出规则迁入 `exit_policy.py`，仓位分配与轮换规则迁入 `position_sizing.py`。
- `strategy_engine.py` 保留兼容导入，已有调用方无需改名，文件减少约 350 行。
- 数据清理必须先创建并校验 gzip 备份；成交台账、订单与账户数据不进入自动保留规则。

## [5.0.0] - 2026-09-25

### Added

- 建立生产代码 Git 基线，默认分支为 `main`。
- 建立正式版本号、统一变更记录和可审计部署清单。
- 纳入 Crypto 与 TradFi 分池的横截面组合、账户级执行保护和退出后同周期禁止重开。

### Security

- 密钥、运行环境、数据库、运行状态、日志、备份和暂存目录不进入版本库。

### Verification

- 基线标签：`v5-baseline-20260925`。
- 基线提交：`45f55ab`。
- 建库时五号既有测试为 `63 passed`。

## 历史记录

建库前的详细策略演进保留在 [docs/V5-CHANGELOG.md](docs/V5-CHANGELOG.md)。
