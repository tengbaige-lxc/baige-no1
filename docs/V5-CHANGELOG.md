# 白鸽五号策略版本改动记录

## 2026-09-25：失效退出后禁止同周期重开

### 版本判断

- 最近两天已平仓收益约 `-6.70 USDT`，胜率 `53.33%`，利润因子仅 `0.40`。
- 主要拖累来自 `AXTI` 空单和 `SPCX` 多单。其中 `AXTI` 在同一冻结目标周期内多次因硬止损或趋势线失效退出，随后又被执行层补回。
- 根因是目标组合虽然会在调仓周期内冻结，但执行状态此前只持久化“保护单失败”冷却，没有记录普通硬止损或结构失效退出。

### 行为变更

1. 硬止损、30 分钟趋势线失效或明确结构失效导致仓位全平后，按“账户 + 标的 + 方向 + 市场池 + 目标周期”记录重开封锁。
2. 同一目标周期内，执行层会过滤该标的同方向候选，不再把刚止损的腿机械补回。
3. Crypto 的封锁在下一个 12 小时目标周期解除；TradFi 的封锁在下一个日度目标周期解除。
4. 封锁写入 `state/execution_state.json`，服务重启后仍然有效。
5. 移动止盈、盈利保护、普通分批减仓和单级背离减仓不会触发封锁；只有确认全平的失效型退出才触发。
6. 状态接口新增 `reentries_blocked`、`exited_target_reentries_blocked`，用于审计被拦截的重复开仓。

### 保持不变

- 不修改当前持仓，不主动平仓，不改变目标组合和调仓时间。
- 不修改 20 倍杠杆、1.5% 软件硬止损、交易所原生止损、仓位上限或多空权重。
- 移动止盈和已有退出层级保持原样。

### 验证

- 项目实际虚拟环境完成 Python 语法检查。
- 五号测试：`63 passed`，仅保留既有 Pydantic 弃用警告。
- 新增覆盖：退出原因分类、账户隔离、状态持久化、下一目标周期自动解锁。
- 上线后 `baige-no5.service=active`，`/health=healthy`。
- 首轮 268 标的扫描于 `2026-09-25 11:48:58 +08:00` 完成，执行结果为 `WAIT`，未新增订单；原有 10 个仓位保持不变。
- `execution_state.json` 已写入 Crypto/TradFi 当前目标周期，状态接口已返回新增审计字段；最近服务日志无 warning。扫描仍有 1 个既有市场数据缺项，不影响本次执行逻辑。

### 备份与文件哈希

- 回滚目录：`/root/baige-no5/backups/v5-reentry-20260925T034225Z`
- `v5_execution.py`: `683f19d736b07accac9511ac46bde4a31cb92cefcd4360007e22cd8cb8a1b145`
- `strategy_engine.py`: `835dc89beba5a160b8880ab46c4a3517618adb1e24a30416aaface799a92938e`
- `test_v5_execution.py`: `10587c0597f8c27624b52ac16034df792e8a79ce4813a8b490c130360d5bc086`

## 历史索引

- `2026-09-18`：五号切换为 Crypto 与 TradFi 分池的横截面多空组合，目标在周期内冻结。
- `2026-09-12`：原生止损覆盖、账户隔离、退出参数接线及相关性约束修复，详见 `V5-safety-fix-20260912.md`。
